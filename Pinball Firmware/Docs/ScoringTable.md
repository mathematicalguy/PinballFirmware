# Scoring Table

| Name | Points | Description |
|------|--------|-------------|
| I/O SPI Port Expansion  | 15 | Implemented a bare-metal SPI driver (`ShiftRegister`) that daisy-chains 3× TPIC6C596 serial-in/parallel-out output ICs and 3× SN74HC165 parallel-in/serial-out input ICs over the ATmega328P hardware SPI bus. Shadow buffers track output state and `writeAll()` / `readAll()` batch-transfer all bytes in one SPI transaction per direction. |
| I/O Debouncing  | 10 | Contact noise is eliminated inside `ShiftRegister` using an 8-sample circular history buffer (one sample every 250 µs via the 4 kHz Timer1 ISR), giving a 2 ms debounce window. An input bit is only promoted to the stable `_debouncedBuf` when all 8 consecutive samples agree, preventing false edge detections on every switch in the system. |
| Flipper Control  | 10 | Each flipper is managed by a three-state (`idle → flipping → holding`) `Flipper` state machine ticked at 4 kHz. On a button press the solenoid receives a 100 ms full-power kick (400 ticks), then drops to a software-PWM hold current to prevent burnout. An end-of-stroke (EOS) switch input cuts the kick phase early if the flipper reaches full travel. |
| Ball Launch  | 5 | `LaunchSolenoid` detects a rising edge on the launch button input (chip 1, pin 4) and drives the launch solenoid output HIGH for a fixed 1-second (4 000-tick) pulse before automatically cutting power, ensuring a consistent ball launch force every time. |
| Score Board Integration  | 10 | Score updates are transmitted to the scoreboard over an RS485 half-duplex bus via the `RS485_USART` class using 9-bit Multiprocessor Communication Mode USART at 250 000 bps. Every time the score changes, `usart.sendScore(score)` is called from the main loop to push the new value, with a DE/RE transceiver direction pin toggled around each transmission. |
| Ramp | 5 | A dedicated ramp-exit sensor (input chip 2, pin 0) is monitored for rising edges inside the Timer1 ISR. Each detected ball passage sets `rampLaneFlag`, which the main loop consumes to award **+10 points** (subject to negative-mode inversion) and immediately sends the updated score to the scoreboard. |
| Top Lanes | 15 | Two top-lane rollover switches (F lane – chip 0 pin 1, Y lane – chip 0 pin 2) each light their corresponding LED (output chip 1 pins 0–1) when rolled over. Either flipper button rotates the lit state between the two lanes. When both LEDs are simultaneously lit a **+50 jackpot** is awarded, both LEDs are held on for a 3-second celebration window (12 000 ticks), then both reset. |
| Hurry-Up Target | 15 | Hitting the right-lane sensor (chip 1, pin 6) opens a 5-second (20 000-tick) hurry-up window and starts flashing an LED at 2 Hz. If the player hits the quick-shot button (chip 0, pin 4) before the window expires they are awarded **+100 points** (one award per window). The LED extinguishes automatically when the window closes. |
| Drop Target Bank | 20 | Three drop targets (chip 2, pins 1–3) are managed by `DropTargetBank`. Each individual target knocked down scores **+10 points**; clearing all three adds a **+50 bonus**. After a full clear the state machine waits 3 seconds (12 000 ticks) then pulses the reset solenoid (chip 0, pin 4) for up to 1 second (4 000 ticks) to raise the targets and return to the watching state. |
| Mode-Based Rule System | 15 | A three-step lane sequence (Top Left → Ball Entry → Right Lane) toggles **negative mode**. In negative mode every score event subtracts points instead of adding them (rolling below zero wraps to 1 000); completing the same sequence again restores positive mode. Any unexpected switch hit between steps resets progress, requiring the full sequence to be replayed. |

**Total: 120 points**
